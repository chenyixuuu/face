# 工程决定

## 2026-09-14

- GitHub 仓库使用 `git@github.com:chenyixuuu/face.git`，主分支为 `main`。
- Git 用户名和邮箱只在当前仓库内配置为 `chenyixuuu`、`chenyixuuu@gmail.com`。
- 用户要求完整工程纳入版本控制，包括数据集、评估报告、模型权重和压缩包。
- 密码、令牌、SSH 私钥及其他登录凭据不得提交。
- 迁移完成并验证 GitHub 推送前，不删除旧工程目录。
- 每次重要会话更新 `memory/CURRENT.md`，并在 `memory/sessions/` 增加一份简短记录。
